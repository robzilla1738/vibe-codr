node_modules/
dist/
*.log
.DS_Store
.env
.env.local
.turbo/
.cache/
coverage/

# vibe-codr runtime state (project config / commands / skills stay tracked)
.vibe/sessions/
.vibe/plans/
.vibe/checkpoints.json
# The semantic memory index is a rebuildable shadow of the markdown in
# .vibe/memory/*.md (which stays tracked so saved facts can be committed).
.vibe/memory/index.sqlite
.vibe/memory/index.sqlite-*
.supergoal/
.vibe/config.json
.vibe/ledger/
