# Memory — 2026-07-11

## 21:49:48 — Project `vibe-codr` is a model-agnostic CLI coding agent (TypeScript/Bun monorepo) featuring a headless engine, TUI, and subagent orchestration with "green-gate" verification. Recently, a Python wrapper `download_yt.py` using `yt-dlp` and `ffmpeg` was created for YouTube downloads. Note: The agent cannot spawn subagents without a specific, goal-oriented task to decompose into disjoint sub-tasks; general requests to "spin up subagents" will be rejected until a concrete objective is provided.
_(session-digest)_

