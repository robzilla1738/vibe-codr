{
  "name": "@vibe/acp",
  "version": "0.7.4",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./client": "./src/process-client.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "lint": "echo \"(no lint configured)\"",
    "build": "echo \"(release builder emits the ACP artifact)\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@agentclientprotocol/sdk": "1.2.1",
    "@vibe/core": "workspace:*",
    "@vibe/protocol": "workspace:*",
    "@vibe/runtime": "workspace:*",
    "@vibe/shared": "workspace:*",
    "zod": "^4.0.0"
  }
}
