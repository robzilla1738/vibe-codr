{
  "name": "@vibe/cli",
  "version": "0.6.4",
  "private": true,
  "type": "module",
  "bin": {
    "vibecodr": "./bin/vibecodr.ts",
    "vibe": "./bin/vibecodr.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "lint": "echo \"(no lint configured)\"",
    "build": "echo \"(bun runs from source)\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@vibe/config": "workspace:*",
    "@vibe/core": "workspace:*",
    "@vibe/providers": "workspace:*",
    "@vibe/shared": "workspace:*",
    "@vibe/tui": "workspace:*"
  }
}
