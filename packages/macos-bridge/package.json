{
  "name": "@vibe/macos-bridge",
  "version": "0.4.34",
  "private": true,
  "type": "module",
  "bin": {
    "vibecodr-engine-host": "./bin/engine-host.ts"
  },
  "exports": {
    ".": "./src/host.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "lint": "echo \"(no lint configured)\"",
    "build": "echo \"(bun runs from source)\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@vibe/config": "workspace:*",
    "@vibe/core": "workspace:*",
    "@vibe/providers": "workspace:*",
    "@vibe/shared": "workspace:*"
  }
}
