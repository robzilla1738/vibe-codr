{
  "name": "@vibe/macos-bridge",
  "version": "0.6.14",
  "private": true,
  "type": "module",
  "bin": {
    "vibecodr-engine-host": "./bin/engine-host.ts"
  },
  "exports": {
    ".": "./src/host.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "lint": "echo \"(no lint configured)\"",
    "build": "echo \"(bun runs from source)\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@vibe/automation": "workspace:*",
    "@vibe/config": "workspace:*",
    "@vibe/core": "workspace:*",
    "@vibe/protocol": "workspace:*",
    "@vibe/providers": "workspace:*",
    "@vibe/runtime": "workspace:*",
    "@vibe/shared": "workspace:*"
  }
}
