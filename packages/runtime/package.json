{
  "name": "@vibe/runtime",
  "version": "0.7.1",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "lint": "echo \"(no lint configured)\"",
    "build": "echo \"(bun runs from source)\"",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@vibe/config": "workspace:*",
    "@vibe/core": "workspace:*",
    "@vibe/protocol": "workspace:*",
    "@vibe/providers": "workspace:*",
    "@vibe/shared": "workspace:*"
  }
}
