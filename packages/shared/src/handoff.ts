/** Compatibility facade. Canonical handoff schemas and inferred types live in @vibe/protocol. */
export type {
  ExecutionTarget,
  CloudSessionStatus,
  PendingCapabilityRequest,
  ExternalCapabilityResolution,
  PortableSessionFileV1,
  PortableSessionArchiveV1,
  HandoffPreparation,
} from "@vibe/protocol";
